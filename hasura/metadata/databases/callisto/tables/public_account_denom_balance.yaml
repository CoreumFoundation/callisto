table:
  name: account_denom_balance
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - address
        - denom
        - amount
        - height
      filter: {}
      allow_aggregations: true
