table:
  name: bridge_transaction
  schema: public

select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - init_height
        - final_height
        - init_hash
        - final_hash
        - counterparty
        - counterparty_hash
        - sender
        - recipient
        - amount
        - direction
        - result
        - operation_ids
      filter: {}
---
table:
  name: bridge_evidence
  schema: public

select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - tx_id
        - height
        - hash
        - sender
        - threshold_reached
      filter: {}