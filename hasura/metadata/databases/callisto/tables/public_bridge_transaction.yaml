table:
  name: bridge_transaction
  schema: public

select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - operation_unique_id
        - height
        - user_initiated_hash
        - source_chain
        - destination_chain
        - issuer
        - sender
        - recipient
        - denom
        - amount
      filter: {}
