table:
  name: bridge_transaction
  schema: public

select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - user_initiated_height
        - user_initiated_hash
        - settlement_hash
        - final_evidence_hash
        - source_chain
        - destination_chain
        - sender
        - recipient
        - amount
        - result
        - operation_id
      filter: {}
