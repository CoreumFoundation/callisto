table:
  name: bridge_evidence
  schema: public

select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - transaction_id
        - height
        - hash
        - relayer_address
        - threshold_reached
      filter: {}