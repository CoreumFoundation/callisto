table:
  name: customparams_params
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - one_row_id
        - staking_params
        - height
      filter: {}
      allow_aggregations: true
